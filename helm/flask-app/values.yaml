replicaCount: 1

image:
  repository: ghcr.io/stariydedd/gitops-k8s-platform
  tag: "4b0a731"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 200m
    memory: 256Mi
